class Player:

    def __init__(self, posX, poY):
        self.position = (posX, poY)

    def move(self, path):
        for i in range(len(path)):
            new_pos = i.get_pos
            self.position = i
